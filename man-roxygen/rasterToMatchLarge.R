#' @param rasterToMatchLarge A `SpatRaster` object to use as the template for all subsequent
#' raster operations (i.e., the one used throughout the simulation).
#' `rasterToMatchLarge` should correspond to `studyAreaLarge` if used.
