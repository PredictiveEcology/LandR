#' @param Nreps the number of simulation replicates/run used to produce summary figures.
#'              NOTE: `mclapply` is used internally, so you should set `options(mc.cores = nReps)`
#'              to take advantage of parallel processing.
