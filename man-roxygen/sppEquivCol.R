#' @param sppEquivCol the column name to use from `sppEquiv` (see [sppEquivalencies_CA]).
#' For functions that have `mixedType`, only necessary if `mixedType == 2`.
#' If `sppEquiv` is not provided and `mixedType == 2`, will use the column of
#' `LandR::sppEquivalencies_CA` that best matches the species names.
