#' @param sppEquiv table with species name equivalencies between naming conventions
#' (see [sppEquivalencies_CA], also `?sppEquiv`).
#' For functions that have `mixedType`, this only necessary if `mixedType == 2`.
#' If not provided and `mixedType == 2`, will attempt to use `LandR::sppEquivalencies_CA`.
