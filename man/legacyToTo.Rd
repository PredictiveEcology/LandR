% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacyToTo.R
\name{.legacyToTo}
\alias{.legacyToTo}
\title{Translate the legacy \code{rasterToMatch} / \code{studyArea} pair into the \verb{*to} family}
\usage{
.legacyToTo(
  to = NULL,
  cropTo = NULL,
  projectTo = NULL,
  maskTo = NULL,
  rasterToMatch = NULL,
  studyArea = NULL,
  useSAcrs = FALSE,
  maskWithRTM = TRUE
)
}
\arguments{
\item{to, cropTo, projectTo, maskTo}{the \verb{*to} family as the caller supplied them.}

\item{rasterToMatch, studyArea}{the legacy pair, usually pulled out of \code{...}.}

\item{useSAcrs}{logical. A \code{studyArea} on its own does not supply the CRS; set this to take
the CRS from it. Ignored unless \code{studyArea} is the only one supplied.}

\item{maskWithRTM}{logical. A \code{rasterToMatch} on its own masks with its own \code{NA}s; set
\code{FALSE} to omit masking. Ignored unless \code{rasterToMatch} is the only one supplied.}
}
\value{
a named list with elements \code{to}, \code{cropTo}, \code{projectTo} and \code{maskTo}, ready to pass
to \code{\link[reproducible:prepInputs]{reproducible::prepInputs()}}. \code{NA} means "omit this step", per \code{reproducible}.
}
\description{
\code{rasterToMatch} and \code{studyArea} are being retired in favour of
\code{\link[reproducible:postProcessTo]{reproducible::postProcessTo()}}'s \code{to}, \code{cropTo}, \code{projectTo} and \code{maskTo}. The old pair is
ambiguous -- what it asks for depends on \emph{which} of the two was supplied -- and that
ambiguity is the reason for the migration. This function is the single place that resolves
it, following the table documented in \code{\link[reproducible:postProcess]{reproducible::postProcess()}}:
}
\details{

}
\keyword{internal}
