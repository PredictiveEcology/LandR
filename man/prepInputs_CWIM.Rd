% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wetland.R
\name{prepInputs_CWIM}
\alias{prepInputs_CWIM}
\title{Wetland (site) layer from the Canadian Wetland Inventory Map}
\usage{
prepInputs_CWIM(
  to,
  url = .cwimURL,
  wetClasses = 1:4,
  wetThreshold = 0.5,
  writeTo = NULL
)
}
\arguments{
\item{to}{A \code{SpatRaster} giving the grid to return, and the study window. Its \code{NA} cells
are \code{NA} in the result.}

\item{url}{The CWIM3A source. A URL is read through GDAL's \verb{/vsicurl/}; a local path is
read directly (useful for a pre-cut regional copy).}

\item{wetClasses}{CWIM3A codes counted as wet. Default: Bog, Fen, Marsh, Swamp.}

\item{wetThreshold}{Minimum wet fraction of a target cell for it to be wet.}

\item{writeTo}{Optional file to write the result to.}
}
\value{
A single-layer \code{SpatRaster} on \code{to}'s grid, named \code{"wetland"}: \code{1} wet, \code{0} not wet.
}
\description{
SCANFI's land cover has no wetland classes, so a land-cover map built from it cannot say
which forest stands on wet ground -- the distinction NTEMS draws with classes 80
(wetland) and 81 (treed wetland). This builds that site layer from the Canadian Wetland
Inventory Map v3A (CWIM3A; Mahdianpari et al. 2021): 10 m, national, 2016-2020 imagery.
}
\details{
Only the study window is read: the source is a cloud-optimised GeoTIFF, so \code{terra} fetches
the tiles it needs rather than the national raster.

CWIM3A codes are 1 Bog, 2 Fen, 3 Marsh, 4 Swamp, 5 Shallow water; 15 is NoData, and CWIM3A
maps nothing but those five classes, so NoData means "not wetland". Shallow water is not
counted as wet by default: it is open water, and it coincides with land-cover water (class
20) almost exactly -- on a 60 km boreal test window, 6,593 of 6,764 CWIM shallow-water cells
were VLCE2 water.

The 10 m cells are summarised onto \code{to} as the \emph{fraction} of each target cell that is wet;
a cell is wet when that fraction is at least \code{wetThreshold}. For a 0/1 layer that is the
majority rule, without \code{"mode"}'s arbitrary tie-breaking.
}
\references{
Mahdianpari, M., et al. (2021). The Third Generation of Pan-Canadian Wetland
Map at 10 m Resolution Using Multisource Earth Observation Data on Cloud Computing
Platform. IEEE JSTARS 14, 8789-8803.
}
\seealso{
\code{\link[=wetlandToLCC]{wetlandToLCC()}}
}
