% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forestLand.R
\name{.forestLandLayer}
\alias{.forestLandLayer}
\title{Prepare one forest-land input once per R session and target geometry}
\usage{
.forestLandLayer(what, lcc, prep)
}
\arguments{
\item{what}{A name for the input, including everything but the geometry that it depends on.}

\item{lcc}{The land cover it is aligned with.}

\item{prep}{A function of no arguments that prepares it.}
}
\value{
A \code{SpatRaster}, backed by a file.
}
\description{
The forest-land inputs are aligned with, but not masked by, the land cover of the year
being prepared, so they depend on its geometry (crs, extent, dimensions) and not on its
values; that year's own \code{NA}s are excluded later, by \code{\link[=.applyForestLand]{.applyForestLand()}}. The result is kept
in \code{.pkgEnv} rather than \code{Cache()}d because it must also be reused when \code{reproducible}'s
Cache is off, e.g. under \code{spades.useCache = "eventsOnly"}.
}
\keyword{internal}
