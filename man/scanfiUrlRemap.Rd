% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scanfiMirror.R
\name{scanfiUrlRemap}
\alias{scanfiUrlRemap}
\title{Resolve SCANFI Google Drive ids to the PredictiveEcology mirror}
\usage{
scanfiUrlRemap()
}
\value{
A remap function for \code{reproducible.urlRemap}, or \code{NULL} if this \code{reproducible} has
no \code{makeUrlRemap()}.
}
\description{
LandR addresses SCANFI v2 files by their Google Drive ids, and some of those ids no longer
resolve for an anonymous user -- the 2020 land cover and 2020 stand age return 404, which
stops the default SCANFI path of \code{Biomass_borealDataPrep}. The same files are mirrored on
the PredictiveEcology object store on arbutus (Digital Research Alliance of Canada). This
builds a \code{\link[reproducible:makeUrlRemap]{reproducible::makeUrlRemap()}} hook from the manifest shipped with LandR, so a
Drive id -- or a SCANFI species \emph{folder} -- is fetched from the mirror instead, with no
Google login and no change at any call site.
}
\details{
LandR sets \code{options(reproducible.urlRemap = scanfiUrlRemap())} when it is loaded, but only
if no remap is set already and \code{getOption("LandR.scanfiMirror", TRUE)} is \code{TRUE}. A remap
of your own is never replaced; if you use one, include these rows in it (the manifest is
\code{system.file("extdata", "arbutus_manifest_SCANFI_v2.csv", package = "LandR")}).
}
