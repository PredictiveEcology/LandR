% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wetland.R
\name{wetlandToLCC}
\alias{wetlandToLCC}
\title{Add wetland classes to a land-cover map}
\usage{
wetlandToLCC(
  lcc,
  wet,
  treedClasses = c(210, 220, 230, 240),
  keepClasses = 20,
  wetCode = 80L,
  wetTreedCode = 81L
)
}
\arguments{
\item{lcc}{Land cover: a \code{SpatRaster} or a numeric vector.}

\item{wet}{Site layer of the same shape: non-zero means wet, \code{0} or \code{NA} not. Typically
\code{\link[=prepInputs_CWIM]{prepInputs_CWIM()}}.}

\item{treedClasses}{Land-cover codes that become \code{wetTreedCode} when wet.}

\item{keepClasses}{Codes never overwritten (default: water).}

\item{wetCode, wetTreedCode}{Codes for wet non-treed and wet treed pixels.}
}
\value{
\code{lcc}, with wet pixels recoded, in the same form it was given.
}
\description{
Marks the pixels a site layer calls wet with the NTEMS wetland codes: \code{wetTreedCode} (81)
where the land cover is treed, \code{wetCode} (80) otherwise. Pixels that are not wet are
returned unchanged, and so are water and any pixel already carrying a wetland code.
}
\details{
"Treed" includes 240 by default: the disturbed-forest code LandR assigns to forest land
that is not currently treed, which is still forest ground.
}
\seealso{
\code{\link[=prepInputs_CWIM]{prepInputs_CWIM()}}
}
