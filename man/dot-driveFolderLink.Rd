% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/driveFallback.R
\name{.driveFolderLink}
\alias{.driveFolderLink}
\title{Link to a folder found by name in a Drive listing}
\usage{
.driveFolderLink(driveLs, driveFolder, shared_drive_url)
}
\arguments{
\item{driveLs}{a \code{dribble}, from \code{\link[googledrive:drive_ls]{googledrive::drive_ls()}} on the shared folder.}

\item{driveFolder}{character; the folder name wanted.}

\item{shared_drive_url}{character; where it was looked for (for the message).}
}
\value{
character(1) folder URL.
}
\description{
The fallback taken when a primary source cannot be reached. It exists only to
fail loudly when the folder is not in the listing: \code{drive_link(character(0))}
is \code{character(0)}, and the \code{drive_ls()} that follows reports only "Parent
specified via \code{path} is invalid: Does not exist", which says nothing about
which folder was wanted or where. The link itself comes from
\code{\link[googledrive:drive_link]{googledrive::drive_link()}} on the matching row, which reads the \code{webViewLink}
that \code{drive_ls()} already fetched -- no further Drive call.
}
\keyword{internal}
