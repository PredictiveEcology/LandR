% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SummarizeRasterFigures.R
\name{generate_polygon_figures}
\alias{generate_polygon_figures}
\title{Generate Summary Figures for Raster Data by Polygon}
\usage{
generate_polygon_figures(
  raster,
  polygons,
  counts_df,
  reference_polygon = NULL,
  output_dir = "polygon_figures",
  remove_zeroes = FALSE,
  aggregate_factor = 1,
  force_overwrite = FALSE,
  value_column = "value",
  count_column = "count"
)
}
\arguments{
\item{raster}{SpatRaster. The raster containing values to be summarized.}

\item{polygons}{SpatVector or sf object. The vector layer containing polygons.}

\item{counts_df}{Data frame. A dataframe with counts of raster values per polygon, such as generated by terra::extract. Polygon ID must match that from polygons.}

\item{reference_polygon}{Optional. SpatVector or sf object. A polygon used as a reference map for inset plotting.}

\item{output_dir}{Character. Directory where output figures will be saved.}

\item{remove_zeroes}{Logical. If TRUE, zero values will be removed from the raster and counts.}

\item{aggregate_factor}{Numeric. Factor by which to aggregate the raster resolution.}

\item{force_overwrite}{Logical. If TRUE, existing files will be overwritten.}

\item{value_column}{Character. Name of the column in counts_df representing raster values. Default is "value".}

\item{count_column}{Character. Name of the column in counts_df representing pixel counts. Default is "count".}
}
\value{
Saves PNG files to the specified output directory. No return value.
}
\description{
This function creates and saves figures for each polygon in a vector layer,
including a histogram of raster values, a map of raster values within the polygon,
and a summary of statistics (min, max, median, mean, 25th percentile, 75th percentile,
and proportion of zeroes). Optionally, zero values can be removed from the analysis,
and the raster can be aggregated to a lower resolution to speed up plotting.
}
