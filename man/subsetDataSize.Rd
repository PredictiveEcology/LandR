% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subsetDataSize.R
\name{subsetDataSize}
\alias{subsetDataSize}
\title{How many rows to keep per group when subsampling for model fitting}
\usage{
subsetDataSize()
}
\value{
A single number: the maximum rows per group to keep.
}
\description{
\code{\link[=subsetDT]{subsetDT()}} keeps at most this many rows per group (e.g., per \code{ecoregionGroup} x
\code{speciesCode}) when fitting the statistical models, and the \code{LandR} modules use it as the
default of their \code{subsetData*Model} parameters. It is a function, and the number behind it
lives once in \code{\link[=LandROptions]{LandROptions()}}, so a project can move it in one place with
\code{options(LandR.subsetDataSize = ...)} and every module follows.
}
\details{
The default is 500. It was 50 for years, chosen when the fits were computationally
expensive; that subsample was small enough that repeated runs of the same simulation gave
visibly different \code{maxB} (a median coefficient of variation of 10\% across
ecoregion x species, up to 62\%, on a 60 km boreal test window). Ten times as many rows is
well within what current machines fit comfortably.
}
\seealso{
\code{\link[=subsetDT]{subsetDT()}}
}
