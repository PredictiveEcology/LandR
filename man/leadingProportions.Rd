% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leadingProportions.R
\name{mixedwoodProp}
\alias{mixedwoodProp}
\alias{leadingProportions}
\alias{leadingSpeciesProp}
\title{Leading-species and mixedwood proportion thresholds}
\usage{
mixedwoodProp()

leadingSpeciesProp()
}
\value{
A numeric proportion between 0 and 1.
}
\description{
Two proportions decide how a stand's composition is labelled, and they answer different
questions. Both are read from options so a project sets them once.
}
\details{
\code{mixedwoodProp()} is the \strong{group} threshold: the share of the stand held by all conifers, or
by all broadleaves, at or above which the stand stops being mixedwood. It is the definition
the national products are written on -- NTEMS/EOSD (Wulder & Nelson 2003) and the NFI photo
plot dictionary call a stand coniferous or broadleaf at 75\% or more of total basal area (NFI
photo plots: total tree volume), and mixed wood when neither group reaches it.

\code{leadingSpeciesProp()} is the \strong{single-species} threshold: the share one species must hold to
be called leading. It is a different question and a user may reasonably want a different
answer -- 0.51, "just a majority", is a sensible setting -- so it has its own option. When it
is not set it takes the mixedwood value, which is what makes the two agree by default.

Set them with, e.g., \code{options(LandR.mixedwoodProp = 0.7)} or
\code{options(LandR.leadingSpeciesProp = 0.51)}. The shipped default for \code{LandR.mixedwoodProp} lives
in \code{\link[=LandROptions]{LandROptions()}} and is the only place the number itself is written down, so changing it
later changes every function at once.

Deciduous conifers are conifers here. \code{Larix} is \code{Type == "Conifer"} in
\link{sppEquivalencies_CA}, so it never counts toward the broadleaf share.
}
\seealso{
\code{\link[=vegTypeMapGenerator]{vegTypeMapGenerator()}}, \code{\link[=lccMapGenerator]{lccMapGenerator()}}
}
